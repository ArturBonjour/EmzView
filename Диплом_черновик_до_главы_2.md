# Введение

Актуальность темы выпускной квалификационной работы обусловлена ростом объема цифрового видеоконтента и усложнением выбора для конечного пользователя. Современные онлайн-платформы предоставляют тысячи фильмов и сериалов, однако стандартные механизмы поиска и фильтрации не всегда обеспечивают персонализированный и быстрый подбор релевантных произведений. В результате пользователь сталкивается с информационной перегрузкой, а качество взаимодействия с сервисом снижается.

В условиях высокой конкуренции цифровых продуктов ценность рекомендательных систем существенно возрастает. Рекомендательный модуль влияет не только на удобство использования, но и на ключевые продуктовые метрики: глубину взаимодействия, возвращаемость аудитории, удовлетворенность пользователя и время принятия решения. Поэтому разработка интеллектуальной системы рекомендаций, учитывающей персональные предпочтения и контекст, является практически значимой задачей.

Существующие подходы к рекомендациям обычно опираются либо на контентный анализ (жанры, описания, актерский состав), либо на коллаборативные методы (поведение пользователей со схожим вкусом). Каждый из подходов имеет ограничения: контентные методы хуже учитывают коллективные паттерны, а коллаборативные — менее устойчивы при холодном старте нового пользователя. В этой связи актуальным является гибридный подход, объединяющий сильные стороны обеих стратегий.

Проблема исследования заключается в необходимости проектирования и реализации системы, которая:
- формирует персонализированные рекомендации фильмов и сериалов;
- адаптируется к холодному старту;
- предоставляет объяснимые рекомендации;
- обеспечивает удобный многоканальный пользовательский опыт (web + Telegram).

Объект исследования — процессы персонализации контента в информационных системах рекомендаций.

Предмет исследования — методы, алгоритмы и программные средства построения гибридной рекомендательной системы фильмов и сериалов с поддержкой объяснимости и мультиплатформенного взаимодействия.

Целью работы является разработка и реализация гибридной рекомендательной системы фильмов и сериалов, обеспечивающей персонализированную выдачу, устойчивую работу при холодном старте и удобный пользовательский опыт на веб-платформе и в Telegram-боте.

Для достижения поставленной цели сформулированы следующие задачи:
1. Проанализировать предметную область рекомендательных систем и выявить ограничения существующих решений для сценариев выбора фильмов и сериалов.
2. Сформировать функциональные и нефункциональные требования к системе рекомендаций с учетом пользовательских и технологических ограничений.
3. Обосновать выбор гибридного подхода на основе контентной и коллаборативной фильтрации, а также механизма объяснимых рекомендаций.
4. Определить состав и структуру данных, необходимых для построения пользовательского профиля и выдачи рекомендаций.
5. Выполнить аналитический обзор архитектурных решений для многосервисной реализации (frontend, backend, ML-сервис, Telegram-бот).
6. Подготовить теоретическую и методологическую основу для последующего проектирования программного продукта.

Методы исследования: сравнительный анализ источников, структурно-функциональный анализ, методы проектирования информационных систем, методы машинного обучения для рекомендаций (content-based и collaborative filtering), эмпирический анализ поведения пользовательских сценариев.

Практическая значимость работы состоит в возможности использования разработанного решения как основы цифрового сервиса персонализированного подбора контента, а также как референсной архитектуры для дипломных и прикладных проектов в области рекомендательных систем.

Структурно работа состоит из введения, основной части, заключения, списка использованных источников и приложений. В первой главе рассматриваются теоретико-методологические аспекты и анализ предметной области. Во второй главе (далее по работе) будет представлено проектирование системы. В последующих главах планируются описание реализации и результаты тестирования.

# 1 Аналитический раздел: предметная область, подходы и требования

## 1.1 Анализ предметной области персонализированных рекомендаций

Рекомендательные системы в сфере кино- и видеосервисов решают задачу ранжирования множества объектов по индивидуальной полезности для пользователя. В прикладной постановке это означает построение упорядоченного списка фильмов и сериалов, который минимизирует когнитивные затраты на выбор и увеличивает вероятность позитивной пользовательской реакции.

С точки зрения жизненного цикла пользовательского взаимодействия можно выделить три ключевых режима:
1. Начальный режим (холодный старт): данных о пользователе недостаточно.
2. Адаптивный режим: накоплены оценки и история взаимодействий, система корректирует профиль.
3. Стабилизированный режим: на основе длительной истории формируется предиктивно устойчивая выдача.

Для системы рекомендаций фильмов и сериалов характерны следующие особенности предметной области:
- гетерогенность контента (кино и сериалы имеют разные паттерны потребления);
- влияние эмоционального контекста (настроение пользователя);
- необходимость объяснимости рекомендаций для повышения доверия;
- высокая значимость визуального UX при принятии решения;
- требование мультиплатформенности (веб и мессенджеры).

## 1.2 Обзор существующих подходов к построению рекомендаций

### 1.2.1 Контентная фильтрация (Content-Based)

Контентный подход формирует рекомендации на основе признаков объектов: жанров, ключевых слов, описаний, актерского состава и других метаданных. Преимущества:
- не требует большого числа пользователей для старта;
- прозрачно объясняется через сходство с уже понравившимися объектами;
- устойчив к разреженности пользовательских связей.

Недостатки:
- ограничение в разнообразии выдачи (риск «замыкания» на похожем контенте);
- зависимость от качества и полноты метаданных;
- слабое использование коллективного поведения аудитории.

### 1.2.2 Коллаборативная фильтрация (Collaborative Filtering)

Коллаборативный подход опирается на схожесть пользователей или объектов по историческим оценкам. Преимущества:
- умеет находить неожиданные и неочевидные рекомендации;
- использует коллективный опыт аудитории;
- эффективен при накопленной истории взаимодействий.

Недостатки:
- проблема холодного старта;
- чувствительность к разреженности матрицы взаимодействий;
- нестабильность качества при малом объеме оценок.

### 1.2.3 Гибридный подход

Гибридные системы объединяют контентный и коллаборативный подходы. На этапе холодного старта используется контентная стратегия, а при накоплении взаимодействий — подключается коллаборативный компонент. Такой подход обеспечивает баланс между устойчивостью старта, качеством персонализации и разнообразием рекомендаций.

Для рассматриваемой работы гибридный подход является обоснованным, так как:
- в проекте предусмотрен онбординг с первичными лайками;
- требуется поддержка объяснений «потому что вам понравилось ...»;
- система должна масштабироваться от малого к большому объему данных.

## 1.3 Анализ аналогов и выявление проблем

Анализ существующих сервисов и типовых учебных реализаций показывает, что часто наблюдаются следующие недостатки:
- отсутствие прозрачных объяснений рекомендаций;
- слабый UX при первичном запуске (пользователь не понимает, как «обучить» систему);
- разрыв между каналами взаимодействия (web и bot используют разные профили);
- ограниченная устойчивость при частичных отказах внешних API;
- недостаточная детализация пользовательского контроля (история оценок, изменение предпочтений, настройки).

Для устранения этих ограничений в проекте EMZ View приняты решения:
- реализован onboarding для ускоренного сбора предпочтений;
- добавлены явные лайк/дизлайк действия в карточках;
- синхронизирован профиль оценок между веб-интерфейсом и Telegram-ботом;
- предусмотрены fallback-сценарии для частичной недоступности ML-сервиса;
- реализована страница профиля с историей и статистикой.

## 1.4 Обоснование технологического стека

Выбранный стек ориентирован на разделение ответственности и независимое масштабирование компонентов:

- **Frontend (React + Vite)** — интерактивный интерфейс, быстрый рендер, поддержка современных UX-паттернов.
- **Backend (Node.js + Express + MongoDB)** — централизованная бизнес-логика, авторизация, интеграция внешних сервисов.
- **ML Service (FastAPI + scikit-learn)** — изолированная реализация алгоритмов рекомендаций.
- **Telegram Bot (Telegraf)** — дополнительный канал взаимодействия без дублирования доменной логики.

Преимущество такой архитектуры — модульность: каждый компонент можно развивать независимо, сохраняя единый контракт через API.

## 1.5 Функциональные требования к разрабатываемой системе

Система должна обеспечивать:
1. Регистрацию, аутентификацию и защищенный доступ пользователя.
2. Онбординг с выбором понравившихся фильмов/сериалов.
3. Получение персонализированных рекомендаций в нескольких витринах:
   - «Для вас»;
   - «Похожий вкус»;
   - «По настроению»;
   - «Потому что вам понравилось ...».
4. Поиск фильмов и сериалов и возможность выставления оценок.
5. Хранение истории оценок и отображение пользовательской статистики.
6. Синхронизацию оценок между web и Telegram-ботом.
7. Переход к карточке объекта с детальной информацией.

## 1.6 Нефункциональные требования

Ключевые нефункциональные требования:
- **Производительность**: приемлемое время ответа API для пользовательских сценариев.
- **Надежность**: корректная деградация при недоступности ML-компонента.
- **Безопасность**: авторизация по JWT, ограничение частоты запросов, валидация входных данных.
- **Масштабируемость**: возможность наращивания пользовательской базы и объема контента.
- **Удобство использования (UX)**: понятная навигация, единые сценарии оценки, визуальная обратная связь.
- **Сопровождаемость**: разделение системы на сервисы и явные API-контракты.

## 1.7 Модель данных и информационные сущности

Для решения задачи рекомендаций используются основные сущности:
- **User**: данные пользователя, параметры онбординга, служебные атрибуты.
- **Movie**: метаданные объекта (tmdbId, тип, жанры, описание, актеры, ключевые слова).
- **Rating**: оценка пользователя объекту (`like`/`dislike`) с источником действия.

Связь между сущностями строится по модели «пользователь — оценка — объект». На этой основе формируется профиль пользователя, который передается в ML-сервис для построения рекомендаций.

## 1.8 Выводы по первой главе

В первой главе проведен анализ предметной области и показана необходимость гибридной рекомендательной системы для сценария выбора фильмов и сериалов. Обоснован выбор сочетания контентной и коллаборативной фильтрации, выявлены ограничения типовых решений и определены требования к разрабатываемому программному продукту.

Результаты аналитического этапа формируют основу для следующего этапа работы — проектирования архитектуры системы, API-взаимодействий, моделей данных и ключевых алгоритмических контуров.

# 2 Проектирование системы рекомендаций EMZ View

## 2.1 Архитектурное проектирование

### 2.1.1 Общая архитектурно-технологическая схема

На основании сформулированных требований принята микросервисная архитектура, в которой каждый компонент реализован как самостоятельный процесс с изолированной зоной ответственности и явным API-контрактом. Компоненты взаимодействуют исключительно через HTTP, что позволяет разрабатывать, тестировать и масштабировать их независимо.

Архитектурно-технологическая схема системы EMZ View включает четыре основных уровня:

1. Уровень представления (Frontend) — одностраничное веб-приложение на React + Vite, запускаемое в браузере пользователя. Взаимодействует только с Backend по HTTPS/REST.

2. Уровень API-шлюза (Backend) — сервис на Node.js + Express, который обслуживает все запросы от Frontend и Telegram-бота: аутентификацию, авторизацию, агрегацию данных, интеграцию с ML-сервисом и TMDB API. Хранит данные в MongoDB.

3. Уровень ML (ML Service) — изолированный сервис на Python + FastAPI, реализующий алгоритмы рекомендаций. Получает запросы только от Backend и не имеет прямого доступа к MongoDB Frontend или другим внешним системам.

4. Уровень дополнительного канала (Telegram Bot) — сервис на Node.js + Telegraf, взаимодействующий с пользователями через Telegram. Все бизнес-операции делегируются в Backend по тому же REST API; бот не хранит собственных данных о пользователях.

Схема взаимодействий (Рисунок 2.1):

```
  Браузер пользователя              Telegram
       │                               │
       │ HTTPS/REST                    │ Telegram Bot API
       ▼                               ▼
  ┌──────────────────────────────────────────────────┐
  │                   Backend (Node.js + Express)     │
  │  Auth │ Movies │ Recommendations │ Actions │ Users│
  └──────────────────────────────────────────────────┘
        │                          │
        │ HTTP REST                │ HTTP REST
        ▼                          ▼
  ┌──────────────┐          ┌──────────────────────┐
  │  ML Service  │          │  TMDB API (external) │
  │  (FastAPI)   │          │  (movie metadata)    │
  └──────────────┘          └──────────────────────┘
        │
  ┌──────────────┐
  │   MongoDB    │
  │  (users,     │
  │   movies,    │
  │   ratings)   │
  └──────────────┘
```
Рисунок 2.1 -- Архитектурно-технологическая схема системы EMZ View

Все сервисы упакованы в Docker-контейнеры и запускаются через Docker Compose с единым файлом переменных окружения `.env`. Межсервисное обнаружение использует DNS-разрешение имен Docker-сети.

### 2.1.2 Технологический стек и обоснование выбора

Таблица 2.1 -- Технологический стек системы EMZ View

| Уровень | Технология | Версия | Обоснование выбора |
|---|---|---|---|
| Frontend | React | 18 | Компонентная модель, широкая экосистема, CSR |
| Frontend | Vite | 5 | Быстрая сборка, Hot Module Replacement |
| Backend | Node.js | 20 | Единый язык стека, неблокирующий ввод-вывод |
| Backend | Express | 4 | Минималистичный, гибко расширяется middleware |
| Backend | MongoDB | 6 | Гибкая схема, удобен для разнородных метаданных |
| Backend | Mongoose | 8 | ODM-слой, схемная валидация, индексы |
| Backend | JWT | -- | Stateless аутентификация, удобен для мультисервиса |
| ML Service | Python | 3.11 | Зрелая ML-экосистема |
| ML Service | FastAPI | 0.110 | Автодокументация, асинхронность, Pydantic |
| ML Service | scikit-learn | 1.4 | TF-IDF, SVD, cosine similarity |
| Telegram Bot | Telegraf | 4 | Удобный DSL для Telegram Bot API |
| DevOps | Docker Compose | 2 | Одностраничный запуск всего стека |

### 2.1.3 Проектирование модуля аутентификации и авторизации

Аутентификация пользователей реализована на основе стандарта JSON Web Token (JWT). При регистрации пользователь указывает email, имя и пароль. Пароль хэшируется алгоритмом bcrypt с соль-фактором 12 перед сохранением. Это обеспечивает устойчивость к атакам с использованием предвычисленных таблиц.

При успешной аутентификации Backend генерирует JWT-токен, содержащий `userId` и срок действия. Токен возвращается клиенту и хранится на стороне Frontend в памяти (не в localStorage) для снижения рисков XSS. При каждом защищенном запросе токен передается в заголовке `Authorization: Bearer <token>`.

Для Telegram-бота предусмотрена отдельная точка входа `/api/auth/telegram`, где бот передает `telegramUserId` вместе с серверным разделяемым секретом (`BOT_SHARED_SECRET`). Backend верифицирует секрет и возвращает JWT для пользователя, связанного с данным Telegram-аккаунтом.

## 2.2 Проектирование API

### 2.2.1 REST API Backend

API Backend построен по принципам REST. Все эндпоинты защищены middleware аутентификации (`requireAuth`), кроме эндпоинтов регистрации и входа. Входные данные валидируются библиотекой Zod; ошибки валидации возвращаются с кодом 400.

Таблица 2.2 -- Ключевые эндпоинты Backend API

| Метод | Путь | Описание |
|---|---|---|
| POST | /api/auth/register | Регистрация пользователя |
| POST | /api/auth/login | Вход и получение JWT |
| POST | /api/auth/telegram | Авторизация через Telegram |
| GET | /api/movies/search | Поиск по TMDB API |
| GET | /api/movies/lists/:kind | Подборки (trending, popular, top, now) |
| GET | /api/movies/:mediaType/:tmdbId | Карточка объекта |
| POST | /api/movies/ensure | Обеспечить наличие объекта в базе |
| POST | /api/movies/sync/trending | Массовая синхронизация trending |
| POST | /api/movies/sync/popular | Массовая синхронизация popular |
| POST | /api/actions/rate | Выставить оценку объекту |
| GET | /api/actions/history | История оценок пользователя |
| GET | /api/actions/top-liked | Топ понравившихся объектов |
| GET | /api/actions/recent-liked | Последние лайки |
| GET | /api/actions/my-rating | Своя оценка конкретного объекта |
| GET | /api/recommendations/for-you | Персональные рекомендации |
| GET | /api/recommendations/similar-users | Рекомендации похожих пользователей |
| GET | /api/recommendations/because | Похожее на конкретный объект |
| GET | /api/recommendations/mood | По настроению |
| GET | /api/users/profile | Профиль пользователя |
| GET | /api/users/stats | Статистика взаимодействий |
| POST | /api/users/onboarding | Сохранение результатов онбординга |

Формат ответа рекомендаций (пример для `/api/recommendations/for-you`):

```json
{
  "recommendations": [
    {
      "tmdbId": 550,
      "mediaType": "movie",
      "title": "Fight Club",
      "overview": "...",
      "posterUrl": "https://image.tmdb.org/t/p/w500/...",
      "score": 0.87,
      "explanation": "Рекомендуем, потому что вам понравился \"Se7en\""
    }
  ],
  "strategy": "collaborative_svd"
}
```

Поле `strategy` информирует клиента о том, какой алгоритм использовался при текущем запросе, что позволяет Frontend адаптировать отображение подзаголовков витрин.

### 2.2.2 ML Service API

ML Service предоставляет четыре эндпоинта, доступных только из Backend:

Таблица 2.3 -- API ML Service

| Метод | Путь | Входные данные | Описание |
|---|---|---|---|
| POST | /recommend/for-you | `UserProfile`, `limit` | Персональные рекомендации |
| POST | /recommend/because | `SeedItem`, `limit` | Похожие на конкретный объект |
| POST | /recommend/mood | `mood`, `limit` | По настроению |
| POST | /recommend/similar-users | `UserProfile`, `limit` | User-user CF |
| GET | /health | -- | Проверка работоспособности |

`UserProfile` содержит идентификатор пользователя и список объектов с оценками (`interactions`). Это позволяет ML Service работать полностью stateless, не обращаясь к MongoDB напрямую.

### 2.2.3 Проектирование кэширования

Для снижения нагрузки на ML Service и TMDB API Backend реализует кэширование ответов в памяти. Ключ кэша включает `userId`, подпись текущего состояния оценок (`count:lastUpdateTimestamp`), параметры запроса и лимит. Это гарантирует, что при добавлении новой оценки кэш автоматически инвалидируется.

TTL кэша настраивается через переменную `RECOMMENDATIONS_CACHE_TTL_SEC` (по умолчанию 120 секунд). Для деградированного состояния (ML Service недоступен) TTL снижается до 30 секунд, чтобы быстрее отдать управление после восстановления.

## 2.3 Проектирование базы данных

База данных MongoDB выбрана как документно-ориентированная СУБД, хорошо подходящая для хранения гетерогенных метаданных фильмов и сериалов. В системе используются три основные коллекции.

### 2.3.1 Коллекция `movies`

Хранит метаданные фильмов и сериалов, полученные из TMDB API и обогащенные для нужд ML-модели.

Таблица 2.4 -- Структура документа коллекции `movies`

| Поле | Тип | Описание |
|---|---|---|
| `_id` | ObjectId | Внутренний идентификатор MongoDB |
| `tmdbId` | Number | Идентификатор в TMDB API |
| `mediaType` | String | Тип контента: `"movie"` или `"tv"` |
| `title` | String | Название |
| `overview` | String | Описание |
| `posterPath` | String | Путь к постеру на TMDB CDN |
| `genres` | Array | Жанры: `[{id, name}]` |
| `cast` | Array | Имена актеров (до 15 имен) |
| `keywords` | Array | Ключевые слова (до 30) |
| `releaseDate` | String | Дата выхода |
| `popularity` | Number | Показатель популярности TMDB |
| `embeddingVersion` | String | Версия векторного представления |
| `createdAt` | Date | Метка создания документа |
| `updatedAt` | Date | Метка последнего изменения |

Уникальный составной индекс по `(tmdbId, mediaType)` гарантирует отсутствие дублирующих документов при одновременном наличии фильма и сериала с одинаковым `tmdbId` в TMDB.

### 2.3.2 Коллекция `ratings`

Хранит оценки пользователей. Каждый документ фиксирует факт взаимодействия конкретного пользователя с конкретным объектом контента.

Таблица 2.5 -- Структура документа коллекции `ratings`

| Поле | Тип | Описание |
|---|---|---|
| `_id` | ObjectId | Внутренний идентификатор |
| `userId` | ObjectId | Ссылка на документ пользователя |
| `tmdbId` | Number | Идентификатор объекта в TMDB |
| `mediaType` | String | Тип контента: `"movie"` или `"tv"` |
| `value` | Number | Оценка: `1` (лайк) или `-1` (дизлайк) |
| `source` | String | Источник оценки: `"web"` или `"telegram"` |
| `createdAt` | Date | Метка создания |
| `updatedAt` | Date | Метка последнего изменения |

Составной уникальный индекс `(userId, tmdbId, mediaType)` обеспечивает операцию upsert: повторная оценка одного объекта обновляет существующий документ, а не создает дубль.

### 2.3.3 Коллекция `users`

Хранит учетные данные и профильную информацию пользователей.

Таблица 2.6 -- Структура документа коллекции `users`

| Поле | Тип | Описание |
|---|---|---|
| `_id` | ObjectId | Внутренний идентификатор |
| `email` | String | Email-адрес (уникальный) |
| `passwordHash` | String | Хэш пароля (bcrypt) |
| `name` | String | Отображаемое имя |
| `telegramUserId` | Number | ID в Telegram (опционально) |
| `onboarding.favoriteMovieTmdbIds` | Array | Любимые фильмы из онбординга |
| `onboarding.favoriteTvTmdbIds` | Array | Любимые сериалы из онбординга |
| `onboarding.favoriteGenreIds` | Array | Выбранные жанры |
| `onboarding.completedAt` | Date | Дата завершения онбординга |
| `createdAt` | Date | Метка регистрации |
| `updatedAt` | Date | Метка последнего изменения |

### 2.3.4 Диаграмма связей между коллекциями

Поскольку MongoDB является документно-ориентированной СУБД, жестких внешних ключей не существует. Логические связи реализованы через хранение идентификаторов:

```
users (коллекция)              ratings (коллекция)
┌─────────────────┐           ┌──────────────────────────┐
│ _id: ObjectId   │◄──────────│ userId: ObjectId (ref)   │
│ email           │           │ tmdbId: Number           │──┐
│ passwordHash    │           │ mediaType: String        │  │
│ name            │           │ value: -1 | 1            │  │
│ telegramUserId  │           │ source: web | telegram   │  │
│ onboarding {...}│           └──────────────────────────┘  │
└─────────────────┘                                          │
                               movies (коллекция)            │
                               ┌───────────────────────┐    │
                               │ _id: ObjectId         │◄───┘
                               │ tmdbId: Number        │ (логическая связь
                               │ mediaType: String     │  по tmdbId+mediaType)
                               │ title, overview       │
                               │ genres, cast, keywords│
                               └───────────────────────┘
```
Рисунок 2.2 -- Диаграмма связей между коллекциями MongoDB

## 2.4 Проектирование ML-компонента

### 2.4.1 Общая схема алгоритмического контура

ML Service реализует четыре алгоритмических стратегии. Выбор стратегии для конкретного запроса определяется типом запроса и объемом накопленных данных о пользователе.

Блок-схема выбора стратегии для эндпоинта `/recommend/for-you` (Рисунок 2.3):

```
Получен запрос /recommend/for-you
           │
           ▼
  Подсчитать число взаимодействий
  пользователя (likes + dislikes)
           │
           ▼
  interactions >= COLLAB_MIN_INTERACTIONS?
     ┌──── Да ─────┐      ┌─── Нет ────┐
     │              │      │             │
     ▼              │      ▼             │
  Collaborative     │   Есть лайки?     │
  Filtering (SVD)   │  ┌── Да ──┐       │
     │              │  │        ▼       │
     │              │  │  Content-Based │
     │              │  │  (TF-IDF       │
     │              │  │   profiling)   │
     │              │  │        │       │
     │              │  └────────┘       │
     │              │        │          │
     │              │        ▼          │
     │              │  Нет лайков =>    │
     │              │  Пустой список    │
     │              │                   │
     └──────────────┘───────────────────┘
           │
           ▼
   Исключить дизлайкнутые объекты
           │
           ▼
   Вернуть список рекомендаций
```
Рисунок 2.3 -- Блок-схема алгоритма выбора стратегии

Порог переключения на коллаборативную фильтрацию (`COLLAB_MIN_INTERACTIONS`) задается через переменную окружения и по умолчанию равен 8.

### 2.4.2 Контентная фильтрация на основе TF-IDF

Контентная модель строится на базе TF-IDF (Term Frequency -- Inverse Document Frequency) векторизации объединенного текстового представления каждого объекта из базы. Текстовое представление включает:

```
text = title + overview + genres + cast + keywords
```

Параметры векторизатора: `max_features = 50 000`, `ngram_range = (1, 2)`, `stop_words = "english"`. Применение биграмм позволяет учитывать словосочетания (например, «science fiction», «based on», «feel good»), повышая точность семантического сходства.

Для вычисления рекомендаций по профилю пользователя (холодный старт) строится средний вектор по всем лайкнутым объектам, затем вычисляется косинусное сходство этого вектора с векторами всех объектов в базе:

```
profile_vec = mean(tfidf[liked_index_i] for i in liked_indices)
scores[j] = cosine_similarity(profile_vec, tfidf[j])   ∀j ∉ liked
```

Модель кэшируется в памяти ML Service и перестраивается только при перезапуске сервиса.

### 2.4.3 Коллаборативная фильтрация на основе SVD

При наличии достаточного числа взаимодействий применяется матричная факторизация через сингулярное разложение (SVD). Матрица рейтингов `R` имеет размерность `|users| x |items|` и содержит значения 1 (лайк) и -1 (дизлайк). Для новых или неоцененных пар значение равно 0.

```
R ≈ U * Σ * Vᵀ

user_factors = U[:, :k] * Σ[:k]      (k = 32 по умолчанию)
item_factors = Vᵀ[:k, :].T
```

Оценка рекомендации для пользователя `u` и объекта `i`:

```
score(u, i) = user_factors[u] · item_factors[i]
```

Пользователи с ненулевым произведением получают ненулевую оценку. Отсортированный список объектов с наибольшим `score`, не содержащихся в истории взаимодействий пользователя, образует итоговые рекомендации.

### 2.4.4 User-User коллаборативная фильтрация

Для витрины «Похожий вкус» реализован метод пользователь-пользователь. Алгоритм:

1. Загрузить матрицу рейтингов из MongoDB.
2. Вычислить косинусное сходство между вектором целевого пользователя и всеми остальными строками матрицы.
3. Выбрать `k_neighbors = 30` наиболее похожих пользователей с `similarity > 0`.
4. Агрегировать оценки объектов, понравившихся соседям, с весом `similarity`:

```
score(item) = sum(similarity(neighbor_j) for j : item liked by neighbor_j)
```

5. Исключить объекты из истории целевого пользователя и вернуть топ по убыванию оценки.

### 2.4.5 Рекомендации по настроению

Витрина «По настроению» реализована как частный случай контентного поиска по заданному текстовому запросу. Для каждого настроения определен набор ключевых слов:

Таблица 2.7 -- Запросы для витрины «По настроению»

| Настроение | Текстовый запрос (английский) |
|---|---|
| Веселое (fun) | `funny comedy uplifting feel good friendship adventure` |
| Грустное (sad) | `sad drama emotional bittersweet tragedy loss` |
| Напряженное (tense) | `tense thriller suspense crime mystery survival` |
| Спокойное (chill) | `calm cozy relaxing slice of life feel good warm peaceful` |

TF-IDF модель векторизует запрос и вычисляет косинусное сходство с векторами всех объектов базы. Это позволяет без дополнительных размеченных данных находить объекты, описания и жанры которых семантически близки к указанному настроению.

## 2.5 UML-диаграммы системы

### 2.5.1 Диаграмма вариантов использования

Система обслуживает два типа акторов: **Пользователь** (взаимодействует через веб-интерфейс или Telegram-бота) и **ML Service** (внутренний актор, автономно вычисляющий рекомендации).

Основные варианты использования для Пользователя:

- UC-01: Зарегистрироваться / войти в систему
- UC-02: Пройти онбординг (выбрать любимые фильмы и жанры)
- UC-03: Получить персональные рекомендации («Для вас»)
- UC-04: Получить рекомендации «Похожий вкус»
- UC-05: Получить рекомендации «Потому что понравилось ...»
- UC-06: Получить рекомендации «По настроению»
- UC-07: Найти фильм/сериал через поиск
- UC-08: Поставить лайк или дизлайк
- UC-09: Просмотреть историю оценок
- UC-10: Просмотреть профиль и статистику
- UC-11: Взаимодействовать с Telegram-ботом (рекомендации, история)

Включение (include):
- UC-03, UC-04, UC-05, UC-06 включают UC-08 (пользователь может оценить объект прямо из рекомендации)
- UC-03, UC-04, UC-05, UC-06 требуют выполнения аутентификации (UC-01)

### 2.5.2 Диаграмма последовательности: получение персональных рекомендаций

```
Пользователь   Frontend   Backend          ML Service   MongoDB    TMDB API
    │              │          │                │            │          │
    │ Открыть      │          │                │            │          │
    │ страницу ──►│          │                │            │          │
    │              │ GET /api/│                │            │          │
    │              │ recomm.. │                │            │          │
    │              │ ─────────►                │            │          │
    │              │          │ load ratings   │            │          │
    │              │          │ ──────────────────────────►│          │
    │              │          │◄──────────────────────────┤          │
    │              │          │ POST /recommend/for-you    │          │
    │              │          │ ────────────────►          │          │
    │              │          │◄────────────────           │          │
    │              │          │ hydrate (load movies)      │          │
    │              │          │ ──────────────────────────►│          │
    │              │          │◄──────────────────────────┤          │
    │              │          │ [если объекты не найдены]  │          │
    │              │          │ GET /movie/:id ───────────────────────►│
    │              │          │◄──────────────────────────────────────┤
    │              │          │ rewrite explanations (LLM, опционально)│
    │              │◄─────────│                │            │          │
    │◄─────────────│          │                │            │          │
    │ Отображение  │          │                │            │          │
    │ витрины      │          │                │            │          │
```
Рисунок 2.4 -- Диаграмма последовательности: персональные рекомендации

### 2.5.3 Диаграмма последовательности: выставление оценки

```
Пользователь   Frontend   Backend         MongoDB    TMDB API
    │              │          │              │          │
    │ Нажать «Лайк»│          │              │          │
    │ ────────────►│          │              │          │
    │              │ POST     │              │          │
    │              │ /api/    │              │          │
    │              │ actions/ │              │          │
    │              │ rate ────►              │          │
    │              │          │ findOne(     │          │
    │              │          │ tmdbId,      │          │
    │              │          │ mediaType) ──►          │
    │              │          │◄─────────────          │
    │              │          │ [если нет]:            │
    │              │          │ GET /movie/:id ────────►│
    │              │          │ Movie.create() ─────────►│
    │              │          │◄────────────────────────│
    │              │          │ Rating.upsert() ─────────►│
    │              │          │◄─────────────           │
    │              │◄─────────│              │          │
    │◄─────────────│          │              │          │
    │ Обновить     │          │              │          │
    │ UI (сердечко)│          │              │          │
```
Рисунок 2.5 -- Диаграмма последовательности: выставление оценки

### 2.5.4 Диаграмма компонентов

На уровне компонентов система делится на следующие блоки:

**Frontend (React SPA):**
- `AuthProvider` -- управление состоянием аутентификации
- `AppShell` -- навигация, тема, глобальный поиск
- `HomePage` -- витрины рекомендаций, lazy loading строк
- `PosterCard` -- универсальная карточка с оценкой
- `OnboardingPage` -- онбординг-флоу
- `ProfilePage` -- история и статистика
- `SettingsPage` -- переключение темы и настройки

**Backend (Node.js):**
- `authRouter` -- регистрация, вход, Telegram-авторизация
- `moviesRouter` -- поиск, карточки, синхронизация
- `actionsRouter` -- оценки, история
- `recommendationsRouter` -- агрегация рекомендаций из ML
- `usersRouter` -- профиль, онбординг, статистика
- `mlClient` -- HTTP-клиент к ML Service
- `cache` -- in-memory TTL-кэш
- `llm` -- опциональная переформулировка объяснений

**ML Service (Python):**
- `ContentModel` -- TF-IDF векторайзер + матрица признаков
- `CollabModel` -- SVD-факторизация матрицы рейтингов
- `hybrid` -- маршрутизация между стратегиями

## 2.6 Проектирование пользовательских сценариев и UX-флоу

### 2.6.1 Сценарий первого запуска (онбординг)

Для решения проблемы холодного старта разработан онбординг-флоу, позволяющий пользователю быстро сформировать начальный профиль. Сценарий состоит из трех шагов:

1. Выбор любимых жанров (фильмы и/или сериалы).
2. Выбор понравившихся объектов из кураторской подборки по выбранным жанрам.
3. Сохранение профиля и переход на главную страницу с уже персонализированной лентой.

После завершения онбординга выбранные объекты автоматически получают оценку «лайк» в коллекции `ratings`, что немедленно учитывается при первом запросе рекомендаций.

### 2.6.2 Сценарий взаимодействия через Telegram-бот

Пользователь взаимодействует с ботом через командное меню:

1. `/start` -- авторизация через Backend по Telegram ID. Если пользователь новый -- запрос email для создания аккаунта.
2. Кнопка «Что посмотреть?» -- запрос персональных рекомендаций через `/api/recommendations/for-you`.
3. Кнопка «Выбрать настроение» -- выбор из четырех вариантов (веселое/грустное/напряженное/спокойное) и запрос через `/api/recommendations/mood`.
4. Кнопка «История» -- отображение последних 5 оценок через `/api/actions/history`.
5. Кнопки лайк/дизлайк -- выставление оценки через `/api/actions/rate`, синхронизируемое с веб-профилем.

Ключевое проектное решение: бот не хранит пользовательских данных и не реализует самостоятельной бизнес-логики. Весь профиль и история хранятся в Backend, что обеспечивает единое пространство данных для обоих каналов.

### 2.6.3 Проектирование состояний интерфейса

При проектировании Frontend определены следующие ключевые состояния для каждой витрины рекомендаций:

- **loading** -- анимированные skeleton-заглушки (сохраняет макет, снижает воспринимаемое время ожидания);
- **loaded** -- горизонтальная прокрутка карточек с постерами;
- **empty** -- нулевое состояние с призывом к действию (добавить оценки);
- **error** -- кнопка повтора запроса к соответствующей витрине;
- **loading more** -- индикатор при подгрузке следующей страницы.

Такое явное проектирование состояний исключает ситуации, при которых пользователь не понимает, загружается контент или просто отсутствует.

## 2.7 Проектирование механизма объяснимости рекомендаций

Объяснимость является ключевым нефункциональным требованием системы. В рамках проектирования определены два уровня объяснений.

**Уровень 1 -- шаблонные объяснения.** ML Service формирует текстовый шаблон:

- «Рекомендуем, потому что вам понравилось {название_seed_объекта}» (content-based по профилю)
- «Похоже на {название_seed_объекта} по жанрам/описанию/актерам» (because-витрина)
- «Рекомендуем, потому что это нравится пользователям с похожим вкусом» (user-user CF)
- «Под настроение: {mood}» (mood-витрина)

Backend при гидрации заменяет числовой `tmdbId` в объяснении на реальное название объекта.

**Уровень 2 -- LLM-переформулировка (опционально).** При наличии переменной `LLM_ENABLED=true` Backend передает шаблонные объяснения в LLM (поддерживается OpenAI API-совместимый интерфейс, по умолчанию -- локальный Ollama). LLM переформулирует объяснение в более естественный язык. Переформулировка выполняется параллельно с управляемой конкурентностью (`LLM_CONCURRENCY`, по умолчанию 4). При ошибке LLM-переформулировки используется исходный шаблон.

## 2.8 Выводы по второй главе

В рамках второй главы разработан полный проект системы EMZ View. Определена и обоснована микросервисная архитектура с разделением на четыре независимых компонента: Frontend, Backend, ML Service и Telegram-бот. Спроектированы API-контракты между компонентами, структуры данных трех коллекций MongoDB с учетом ограничений уникальности и логических связей между ними.

Детально спроектирован алгоритмический контур ML-компонента: обоснован механизм выбора стратегии (контентная или коллаборативная) в зависимости от объема накопленных данных пользователя, описаны математические основы TF-IDF профилирования, SVD-матричной факторизации и user-user коллаборативной фильтрации. Определены четыре витрины рекомендаций и механизм их формирования.

Разработаны UML-диаграммы ключевых сценариев взаимодействия и диаграмма компонентов системы. Спроектированы пользовательские сценарии онбординга, Telegram-бот-флоу и механизм объяснимости на двух уровнях.

Полученные результаты проектирования являются основой для третьей главы, в которой описывается непосредственная программная реализация разработанных проектных решений.

# 3 Программная реализация системы рекомендаций EMZ View

## 3.1 Реализация Backend-сервиса

### 3.1.1 Структура Backend-приложения и точка входа

Backend-сервис реализован на платформе Node.js 20 с использованием фреймворка Express 4. Точка входа (`src/index.js`) выполняет инициализацию в следующем порядке:

1. Загрузка переменных окружения из корневого файла `.env` через `dotenv`.
2. Создание экземпляра Express-приложения с отключенным заголовком `ETag` (для предотвращения нежелательного кэширования клиентом).
3. Подключение middleware: JSON-парсер с ограничением тела 1 МБ, логгер HTTP-запросов (Morgan), CORS с настраиваемым списком допустимых источников, глобальный rate limiter (300 запросов в минуту на IP).
4. Подключение заголовка `Cache-Control: no-store` для всех `/api`-маршрутов.
5. Регистрация роутеров: `authRouter`, `moviesRouter`, `actionsRouter`, `recommendationsRouter`, `usersRouter`.
6. Регистрация обработчиков 404 и глобального обработчика ошибок.
7. Подключение к MongoDB и запуск HTTP-сервера на порту 8080.

Схема точки входа Backend (рис. 3.1):

```
index.js
├── dotenv.config()
├── express()
│   ├── express.json({ limit: '1mb' })
│   ├── morgan('dev')
│   ├── cors({ origin: CORS_ORIGIN })
│   ├── rateLimit(300 req/min)
│   ├── GET /health
│   ├── /api/auth    → authRouter
│   ├── /api/movies  → moviesRouter
│   ├── /api/actions → actionsRouter
│   ├── /api/recommendations → recommendationsRouter
│   └── /api/users   → usersRouter
├── notFoundHandler
├── errorHandler
└── connectMongo() → app.listen(:8080)
```
Рисунок 3.1 -- Структура точки входа Backend

### 3.1.2 Реализация модуля аутентификации

Маршруты аутентификации (`src/routes/auth.js`) реализуют пять эндпоинтов: регистрация, вход, авторизация через Telegram, запрос сброса пароля и выполнение сброса пароля.

Каждый эндпоинт снабжен отдельным rate limiter:
- Регистрация: 15 запросов за 30 минут.
- Вход: 20 запросов за 10 минут.
- Запрос и выполнение сброса пароля: по 15 запросов за 15 минут.

Входные данные каждого запроса проходят валидацию через схему Zod. При несоответствии формата возвращается ответ 400 с описанием ошибки валидации. Этот подход устраняет необходимость в ручных проверках и обеспечивает единообразную обработку ошибок.

Пароль хэшируется алгоритмом bcrypt с коэффициентом стойкости `saltRounds = 10`. После успешной регистрации или входа формируется JWT-токен с полезной нагрузкой `{ userId, email, name }` и настраиваемым сроком действия (по умолчанию 7 дней).

Сброс пароля реализован токенным механизмом: генерируется криптографически стойкий токен через `crypto.randomBytes(24)`, его SHA-256-хэш сохраняется в документе пользователя вместе со временем истечения (30 минут). При выполнении сброса хэш полученного токена сравнивается с сохраненным — при совпадении пароль обновляется, а токен аннулируется.

Авторизация Telegram-бота (`POST /api/auth/telegram`) проверяет `BOT_SHARED_SECRET` — разделяемый секрет, известный только серверу и боту. При первом подключении неизвестного `telegramUserId` бот передает email пользователя, и создается новый аккаунт либо существующий аккаунт связывается с Telegram ID.

### 3.1.3 Реализация маршрутов рекомендаций и кэширования

Ключевой особенностью модуля рекомендаций является детерминированная инвалидация кэша на основе подписи состояния оценок. Функция `getRatingsSignature` вычисляет строку вида `{count}:{lastUpdateISO}`:

```javascript
async function getRatingsSignature(userId) {
  const [count, last] = await Promise.all([
    Rating.countDocuments({ userId }),
    Rating.findOne({ userId }).sort({ updatedAt: -1 }).lean(),
  ]);
  const t = last?.updatedAt
    ? new Date(last.updatedAt).toISOString()
    : 'none';
  return `${count}:${t}`;
}
```

Такой подход гарантирует инвалидацию кэша при каждом новом лайке или дизлайке, без необходимости использовать внешний брокер сообщений.

Эндпоинт `GET /api/recommendations/because` реализует каскадный механизм fallback: сначала выполняется запрос к ML Service, и если результатов недостаточно (менее `min(8, limit/2)`) — автоматически выполняется резервный запрос к TMDB API (`/movie/:id/recommendations` или `/tv/:id/recommendations`). Это обеспечивает непрерывность работы витрины при малом объеме данных в локальной базе.

Функция `hydrateRecommendations` обогащает список объектов метаданными из MongoDB. Если часть объектов не найдена в локальной базе, они запрашиваются у TMDB API и сохраняются в MongoDB через `Movie.updateOne(..., { upsert: true })`. Это обеспечивает автоматическое наполнение локальной базы при каждом запросе рекомендаций.

### 3.1.4 Реализация маршрутов работы с объектами (Movies)

Модуль `movies.js` реализует вспомогательную функцию `ensureMovie`, которая является центральным механизмом пополнения локальной базы:

```javascript
async function ensureMovie({ tmdbId, mediaType }) {
  const existing = await Movie.findOne({ tmdbId, mediaType });
  if (existing) return existing;

  const { data } = await tmdb.get(endpoint, {
    params: { append_to_response: 'credits,keywords' },
  });
  return Movie.create({
    tmdbId, mediaType, title, genres, cast, keywords, ...
  });
}
```

Функция вызывается из нескольких контекстов: при просмотре карточки объекта, при выставлении оценки, при синхронизации trending/popular через соответствующие эндпоинты. Параметр `append_to_response: 'credits,keywords'` позволяет получить актерский состав и ключевые слова за один запрос к TMDB API, что минимизирует число обращений к внешнему сервису.

Для списков (trending, popular, top, now) реализовано кэширование в памяти с TTL 5 минут. Для жанров TTL составляет 24 часа, поскольку они изменяются крайне редко.

### 3.1.5 Реализация маршрутов онбординга

Эндпоинт `PUT /api/users/onboarding` выполняет несколько операций в одной транзакции:

1. Обновление поля `onboarding` в документе пользователя (любимые фильмы, сериалы, жанры, дата завершения).
2. Для каждого выбранного объекта (не более 30): проверка наличия в локальной базе, при отсутствии -- запрос к TMDB API и создание документа в `movies`.
3. Создание записи `Rating` с `value: 1` (лайк) для каждого выбранного объекта через `findOneAndUpdate(..., { upsert: true })`.

Операция `upsert` гарантирует идемпотентность: повторный вызов онбординга не приведет к дублированию оценок.

Эндпоинт кандидатов для онбординга (`GET /api/users/onboarding/candidates`) параллельно запрашивает шесть списков из TMDB API (trending/popular/top_rated для фильмов и сериалов), фильтрует объекты с недостаточным числом оценок или без постера, дедуплицирует по нормализованному названию и возвращает отсортированный по популярности список.

## 3.2 Реализация Frontend-приложения

### 3.2.1 Структура Frontend-приложения

Frontend реализован как одностраничное приложение (SPA) на React 18 с Vite 5 в качестве сборщика. Структура проекта:

```
frontend/src/
├── main.jsx          # точка входа React
└── ui/
    ├── App.jsx       # корневой компонент, роутинг, состояние авторизации
    ├── styles.css    # глобальные CSS-переменные и компонентные стили
    ├── lib/
    │   ├── api.js    # axios-клиент, управление токеном
    │   └── toast.js  # уведомления
    ├── pages/
    │   ├── LandingPage.jsx    # лендинг для неаутентифицированных
    │   ├── LoginPage.jsx      # форма входа
    │   ├── RegisterPage.jsx   # форма регистрации
    │   ├── OnboardingPage.jsx # онбординг-сетка
    │   ├── HomePage.jsx       # главная (витрины рекомендаций)
    │   ├── CollectionsPage.jsx # тематические подборки
    │   ├── DetailPage.jsx     # карточка фильма/сериала
    │   ├── ProfilePage.jsx    # профиль пользователя
    │   ├── SearchPage.jsx     # результаты поиска
    │   └── SettingsPage.jsx   # настройки
    └── components/
        ├── PosterCard.jsx    # карточка с рейтингом
        ├── Row.jsx           # горизонтальная строка карточек
        ├── Toasts.jsx        # компонент уведомлений
        └── ErrorBoundary.jsx # перехват JS-ошибок
```

Маршрутизация реализована через React Router 6. Защищенные маршруты проверяют наличие JWT-токена в памяти и статус завершения онбординга: при `onboardingCompleted === false` пользователь перенаправляется на `/onboarding`.

### 3.2.2 Управление темой оформления

Переключение между темной и светлой темами реализовано через атрибут `data-theme` корневого элемента `<html>`. CSS-переменные (custom properties) задают различные значения цветов для каждого значения атрибута:

```css
:root[data-theme="dark"] {
  --bg: #0f0f0f;
  --surface: #1a1a1a;
  --text: #f5f5f5;
  /* ... */
}
:root[data-theme="light"] {
  --bg: #f5f5f5;
  --surface: #ffffff;
  --text: #111111;
  /* ... */
}
```

Выбор темы сохраняется в `localStorage` и применяется при следующей загрузке страницы. При переключении добавляется атрибут `data-theme-transition` на 520 мс, что позволяет реализовать CSS-переход для плавной смены темы. Начальная тема определяется по системным предпочтениям пользователя (`prefers-color-scheme: dark`).

### 3.2.3 Реализация компонента PosterCard

Компонент `PosterCard` является центральным интерактивным элементом интерфейса. Реализован с использованием `React.memo` для предотвращения лишних перерисовок в длинных горизонтальных строках.

Компонент реализует три уровня кэширования состояния рейтинга:

1. **Кэш модуля** (`ratingCache: Map`) -- сохраняет последний известный рейтинг объекта между монтированиями компонентов.
2. **In-flight деduplication** (`inflightRating: Map`) -- гарантирует, что для одного объекта одновременно выполняется не более одного HTTP-запроса к `/api/actions/my-rating`.
3. **CustomEvent `emz:rating`** -- при выставлении оценки компонент публикует глобальное событие, которое обновляет все карточки с тем же объектом на странице без дополнительных HTTP-запросов.

Функция `rate` реализует оптимистичное обновление UI: значок рейтинга меняется немедленно, а уведомление появляется после завершения запроса. При ошибке отображается toast с сообщением об ошибке.

### 3.2.4 Реализация страницы онбординга

Страница онбординга (`OnboardingPage.jsx`) использует `IntersectionObserver` для реализации бесконечной прокрутки сетки кандидатов. Sentinel-элемент размещается в конце списка; при его попадании в область видимости (с margin 800px) автоматически запрашивается следующая страница кандидатов.

Состояние выбранных объектов хранится в `Map<string, Item>`, где ключ -- строка `{mediaType}:{tmdbId}`. Это обеспечивает O(1) операции добавления и удаления, а также простую проверку на наличие объекта в выборке.

Прогресс-бар показывает, сколько объектов выбрано относительно минимального порога (12). Кнопка «Продолжить» активируется только при достижении порога. Skeleton-заглушки заменяют сетку во время загрузки первой страницы кандидатов.

### 3.2.5 Реализация главной страницы (HomePage)

Главная страница реализует параллельную загрузку шести групп рекомендаций:

- «Для вас» (фильмы и сериалы) -- `/api/recommendations/for-you`
- «Похожий вкус» (фильмы и сериалы) -- `/api/recommendations/similar-users`
- «По настроению» (напряженные, фильмы и сериалы) -- `/api/recommendations/mood?mood=tense`
- «Потому что понравилось...» -- `/api/recommendations/because` для каждого seed-объекта

Каждая группа отображается как горизонтальная строка (`Row`). При недостаточном количестве рекомендаций отображается состояние «empty» с призывом добавить оценки.

Поиск реализован с debounce 250 мс через `setTimeout`. При наборе текста пользователь автоматически перенаправляется на `/search?q=...`. При переходе на другую страницу поисковый запрос очищается.

Количество карточек в строке настраивается через `pageStep` (8–40, по умолчанию 16), значение сохраняется в `localStorage` и применяется через CustomEvent `emz:settings` со страницы настроек.

## 3.3 Реализация ML-сервиса

### 3.3.1 Структура ML-сервиса

ML-сервис реализован как самостоятельное Python-приложение на базе фреймворка FastAPI. Точка входа (`app/main.py`) создает приложение и подключает роутер рекомендаций:

```python
app = FastAPI(title="EMZ ML Service", version="1.0.0")

@app.get("/health")
def health():
    return {"ok": True}

app.include_router(recommend_router, prefix="/recommend")
```

Эндпоинт `/health` используется Docker Compose для проверки готовности контейнера перед тем, как Backend начнет направлять запросы.

### 3.3.2 Реализация контентной модели (TF-IDF)

Контентная модель реализована в `app/services/content.py`. Загрузка данных и построение модели выполняются при первом обращении и кэшируются в глобальной переменной `_CONTENT_MODEL`. Это означает, что стоимость построения модели оплачивается один раз при «прогреве» сервиса.

Текстовое представление каждого объекта формируется конкатенацией полей:

```python
text = f"{title} {overview} {genres} {cast} {keywords}".strip()
```

TF-IDF векторайзер инициализируется с параметрами:

```python
vectorizer = TfidfVectorizer(
    max_features=50_000,
    ngram_range=(1, 2),
    stop_words="english",
)
tfidf = vectorizer.fit_transform(texts)
```

Параметр `max_features=50_000` ограничивает словарь наиболее значимыми термами, снижая размерность пространства признаков. `ngram_range=(1, 2)` позволяет учитывать биграммы, что важно для составных жанровых определений. Результат векторизации (`tfidf`) является разреженной матрицей формата `scipy.sparse.csr_matrix`, что обеспечивает эффективное использование памяти.

Для рекомендации по профилю строится средний вектор лайкнутых объектов:

```python
profile_vec = model.tfidf[liked_indices].mean(axis=0)
sims = cosine_similarity(profile_vec, model.tfidf).flatten()
```

### 3.3.3 Реализация коллаборативной модели (SVD)

Коллаборативная модель в `app/services/collab.py` использует сингулярное разложение матрицы рейтингов через `numpy.linalg.svd`. Матрица строится в памяти из коллекции `ratings` MongoDB:

```python
U, s, Vt = np.linalg.svd(mat, full_matrices=False)
kk = min(k, len(s))
user_factors = U[:, :kk] * s[:kk]   # форма: [n_users, k]
item_factors = Vt[:kk, :].T          # форма: [n_items, k]
```

Параметр `k = 32` определяет размерность латентного пространства. Предсказание рейтинга для пары пользователь-объект выполняется через скалярное произведение факторных векторов:

```python
scores = model.user_factors[uidx] @ model.item_factors.T
```

Эта операция выполняется как матричное умножение вектора на матрицу, что дает оценки для всех объектов за одно вычисление. Объекты, уже оцененные пользователем, исключаются из результата.

### 3.3.4 Реализация User-User коллаборативной фильтрации

Алгоритм user-user CF в `app/services/collab.py` вычисляет косинусное сходство между целевым пользователем и всеми остальными строками матрицы рейтингов:

```python
sims = cosine_similarity(user_vec, mat).flatten()
sims[uidx] = -1.0  # исключить самого себя
neighbor_idx = np.argsort(-sims)[:k_neighbors]
neighbor_idx = [i for i in neighbor_idx if sims[i] > 0]
```

Оценка объекта вычисляется как взвешенная сумма сходств соседей, которые оценили этот объект положительно. Функция возвращает кортеж `(рекомендации, число_реальных_соседей)`, что позволяет Frontend информативно показывать подзаголовок «Похожий вкус» с указанием числа пользователей со схожим вкусом.

### 3.3.5 Реализация маршрутизатора стратегий (hybrid)

Модуль `app/services/hybrid.py` является точкой принятия решений о выборе стратегии. Функция `recommend_for_you` читает значение порога из переменной окружения и применяет логику переключения:

```python
min_collab = int(os.getenv("COLLAB_MIN_INTERACTIONS", "8"))

if len(req.profile.interactions) >= min_collab:
    recs = recommend_for_user(...)
    strategy = "collaborative_svd"
else:
    # холодный старт
    cb = recommend_from_profile(liked, ...)
    strategy = "content_tfidf"
```

Поле `strategy` передается в ответе Backend клиенту. Frontend использует его для выбора подзаголовка витрины: при `content_tfidf` отображается «Для вас (контентные)», при `collaborative_svd` -- «Персонально для вас».

## 3.4 Реализация Telegram-бота

### 3.4.1 Структура и запуск бота

Telegram-бот реализован на Node.js с использованием библиотеки Telegraf 4. Бот работает в режиме long polling (`bot.launch()`), без необходимости настройки webhook.

Для хранения JWT-токенов пользователей используется `Map<number, Session>` в памяти процесса, где ключ -- числовой Telegram ID пользователя:

```javascript
const sessions = new Map();
function getSession(userId) {
  if (!sessions.has(userId)) {
    sessions.set(userId, { jwt: null, pendingEmail: false });
  }
  return sessions.get(userId);
}
```

Это обеспечивает O(1) доступ к сессии. Важно отметить, что при перезапуске контейнера сессии очищаются, и пользователям необходимо повторно авторизоваться через `/start`. Для продакшн-сценария это поведение является допустимым, поскольку авторизация выполняется быстро.

### 3.4.2 Сценарий авторизации

При команде `/start` бот отправляет запрос `POST /api/auth/telegram` с `telegramUserId` и `BOT_SHARED_SECRET`. Возможны три исхода:

1. Пользователь существует и связан с Telegram -- возвращается JWT, открывается главное меню.
2. Пользователь не существует -- Backend возвращает ошибку `email is required`, бот переводит сессию в состояние `pendingEmail = true` и просит ввести email.
3. Пользователь вводит email -- бот отправляет повторный запрос с email и именем (`ctx.from.first_name`).

### 3.4.3 Inline-клавиатура и обработчики действий

Взаимодействие с пользователем организовано через inline-кнопки. Каждая кнопка несет callback data в формате `action:param`. Обработчики действий регистрируются через `bot.action()`:

- `menu:recommend` -- запрос рекомендации через `sendRecommendation()`.
- `menu:mood` -- показ клавиатуры выбора настроения.
- `mood:{fun|sad|tense|chill}` -- запрос рекомендации с параметром `mood`.
- `menu:history` -- загрузка последних 5 оценок.
- `rate:(like|dislike):{tmdbId}:(movie|tv)` -- выставление оценки.

Обработчик `rate` использует регулярное выражение `/^rate:(like|dislike):(\d+):(movie|tv)$/i` для разбора callback data, что исключает необходимость в отдельной таблице состояний для каждого оцениваемого объекта.

Функция `sendRecommendation` строит сообщение с HTML-форматированием (жирный заголовок, обзор до 400 символов, курсивное объяснение) и добавляет кнопки лайк/дизлайк, ссылку на веб-карточку объекта и кнопку «Ещё вариант».

## 3.5 Контейнеризация и DevOps

### 3.5.1 Docker Compose конфигурация

Все четыре сервиса упакованы в Docker-контейнеры и запускаются через `docker-compose.yml`. Конфигурация обеспечивает:

- Единый файл конфигурации `.env` для всех сервисов (`env_file: .env`).
- Изолированную Docker-сеть для межсервисного взаимодействия.
- Постоянный том для MongoDB (`mongo_data`).
- Проверки готовности (`healthcheck`) для ML Service и MongoDB.

Сервисы запускаются командой:

```bash
docker compose up --build -d
```

### 3.5.2 Пользовательский интерфейс: скриншоты

На рисунке 3.2 представлена главная страница веб-приложения с витринами персонализированных рекомендаций в темной теме. Каждая витрина отображается как горизонтальная прокручиваемая строка карточек с постерами, которые при наведении показывают оверлей с кнопками лайк/дизлайк и объяснением рекомендации.

На рисунке 3.3 представлена страница онбординга с сеткой кандидатов. Выбранные объекты выделяются галочкой и рамкой; прогресс-бар в нижней части страницы показывает количество выбранных объектов относительно минимального порога.

На рисунке 3.4 представлен интерфейс Telegram-бота: главное меню с inline-кнопками и карточка рекомендации с объяснением, кнопками оценки и ссылкой на веб-версию.

## 3.6 Выводы по третьей главе

В третьей главе описана программная реализация всех четырех компонентов системы EMZ View. Backend-сервис реализован с многоуровневой защитой (rate limiting, валидация Zod, JWT-авторизация) и механизмом автоматического пополнения локальной базы при каждом запросе. Frontend-приложение обеспечивает реактивный пользовательский опыт через оптимистичные обновления, дебаунсированный поиск и синхронизацию состояния через CustomEvent. ML-сервис реализует три алгоритмических стратегии с автоматическим переключением между ними. Telegram-бот предоставляет полный функционал рекомендаций через inline-интерфейс, используя тот же Backend и профиль пользователя.


# 4 Тестирование системы рекомендаций EMZ View

## 4.1 Стратегия и виды тестирования

Тестирование системы EMZ View проводилось по трем направлениям:

1. **Функциональное тестирование** -- проверка корректности работы каждого эндпоинта Backend API и ML-сервиса по сценариям, соответствующим функциональным требованиям.
2. **Тестирование пользовательского интерфейса** -- проверка UX-сценариев в веб-приложении и Telegram-боте.
3. **Нагрузочное тестирование** -- оценка времени отклика при реалистичной нагрузке.

## 4.2 Функциональное тестирование Backend API

Функциональное тестирование выполнялось методом черного ящика с использованием инструментов HTTP-тестирования (curl / Postman). Для каждого эндпоинта проверялись: корректный сценарий, сценарий с невалидными входными данными и сценарий с нарушением авторизации.

### 4.2.1 Тестирование модуля аутентификации

В таблице 4.1 представлены результаты тестирования эндпоинтов модуля аутентификации.

Таблица 4.1 -- Результаты тестирования модуля аутентификации

| № | Эндпоинт | Тестовый случай | Ожидаемый результат | Фактический результат |
|---|---|---|---|---|
| 1 | POST /api/auth/register | Корректные данные (email, пароль, имя) | 200, JWT-токен | Пройден |
| 2 | POST /api/auth/register | Дублирующийся email | 409, error | Пройден |
| 3 | POST /api/auth/register | Пароль короче 8 символов | 400, ошибка валидации | Пройден |
| 4 | POST /api/auth/login | Корректные учетные данные | 200, JWT-токен | Пройден |
| 5 | POST /api/auth/login | Неверный пароль | 401, Invalid credentials | Пройден |
| 6 | POST /api/auth/login | Более 20 запросов за 10 мин | 429, Too Many Requests | Пройден |
| 7 | POST /api/auth/telegram | Корректный BOT_SHARED_SECRET | 200, JWT-токен | Пройден |
| 8 | POST /api/auth/telegram | Неверный BOT_SHARED_SECRET | 401, Invalid secret | Пройден |
| 9 | POST /api/auth/request-password-reset | Корректный email | 200, ok: true | Пройден |
| 10 | POST /api/auth/reset-password | Корректный токен и новый пароль | 200, ok: true | Пройден |
| 11 | POST /api/auth/reset-password | Истекший токен | 400, Invalid token | Пройден |

### 4.2.2 Тестирование маршрутов оценок

В таблице 4.2 представлены результаты тестирования маршрутов действий пользователя.

Таблица 4.2 -- Результаты тестирования маршрутов оценок

| № | Эндпоинт | Тестовый случай | Ожидаемый результат | Фактический результат |
|---|---|---|---|---|
| 1 | POST /api/actions/rate | Лайк существующего фильма | 200, запись Rating | Пройден |
| 2 | POST /api/actions/rate | Повторный лайк -- upsert | 200, обновление без дубля | Пройден |
| 3 | POST /api/actions/rate | Дизлайк -- смена значения | 200, value: -1 | Пройден |
| 4 | POST /api/actions/rate | Запрос без JWT | 401, Unauthorized | Пройден |
| 5 | GET /api/actions/history | Корректный запрос | 200, массив history | Пройден |
| 6 | GET /api/actions/history | Фильтр value=1 | 200, только лайки | Пройден |
| 7 | GET /api/actions/my-rating | Оцененный объект | 200, value: 1 или -1 | Пройден |
| 8 | GET /api/actions/my-rating | Не оцененный объект | 200, rating: null | Пройден |

### 4.2.3 Тестирование маршрутов рекомендаций

В таблице 4.3 представлены результаты тестирования маршрутов рекомендаций.

Таблица 4.3 -- Результаты тестирования маршрутов рекомендаций

| № | Эндпоинт | Тестовый случай | Ожидаемый результат | Фактический результат |
|---|---|---|---|---|
| 1 | GET /api/recommendations/for-you | Пользователь с 0 оценок | 200, strategy: content_tfidf, [] | Пройден |
| 2 | GET /api/recommendations/for-you | Пользователь с 5 лайками | 200, content_tfidf, список | Пройден |
| 3 | GET /api/recommendations/for-you | Пользователь с 10+ лайками | 200, collaborative_svd | Пройден |
| 4 | GET /api/recommendations/for-you | ML Service недоступен | 200, strategy: unavailable, [] | Пройден |
| 5 | GET /api/recommendations/mood | mood=fun | 200, список | Пройден |
| 6 | GET /api/recommendations/mood | Некорректное значение mood | 400, ошибка валидации | Пройден |
| 7 | GET /api/recommendations/because | Известный tmdbId | 200, because-список | Пройден |
| 8 | GET /api/recommendations/because | Неизвестный tmdbId (fallback TMDB) | 200, tmdb_recommendations | Пройден |
| 9 | GET /api/recommendations/similar-users | Пользователь без соседей | 200, [], neighborCount: 0 | Пройден |

Тестирование деградированного режима (строка 4) показало, что при недоступности ML Service Backend корректно возвращает пустой список с `strategy: "unavailable"`, не генерируя ошибку 5xx. Frontend в этом случае отображает состояние «empty» с призывом добавить оценки.

## 4.3 Тестирование ML-сервиса

### 4.3.1 Тестирование TF-IDF модели

Контентная модель проверялась на консистентность и релевантность рекомендаций. Для валидации использовался метод: заданному seed-объекту (фильм с известным жанровым профилем) запрашивались ближайшие объекты по косинусному сходству. Результаты демонстрировали устойчивую кластеризацию по жанрам и актерскому составу.

В таблице 4.4 приведены примеры результатов функции `recommend_similar` для проверочных seed-объектов (условные данные, демонстрирующие корректность работы).

Таблица 4.4 -- Примеры работы контентной модели

| Seed-объект | Тип | Топ-3 рекомендованных объекта | Обоснование |
|---|---|---|---|
| Криминальное кино | movie | Схожие по жанру и ключевым словам | crime, drama, heist |
| Научная фантастика | movie | Схожие SF-фильмы | sci-fi, space, future |
| Семейный мультфильм | tv | Схожие семейные сериалы | family, animation |

### 4.3.2 Тестирование стратегии холодного старта

Для пользователя с 0 оценок функция `recommend_for_you` корректно возвращает пустой список с `strategy: content_tfidf`. После проведения онбординга (добавление 12+ лайков) в ответе появляются контентные рекомендации. После накопления 8+ взаимодействий стратегия переключается на `collaborative_svd`.

Таблица 4.5 -- Тестирование переключения стратегий

| Число взаимодействий | Ожидаемая стратегия | Фактическая стратегия | Результат |
|---|---|---|---|
| 0 | content_tfidf, пустой список | content_tfidf, [] | Пройден |
| 5 | content_tfidf | content_tfidf | Пройден |
| 8 | collaborative_svd | collaborative_svd | Пройден |
| 15 | collaborative_svd | collaborative_svd | Пройден |

## 4.4 Тестирование пользовательского интерфейса

### 4.4.1 Тестирование сценария регистрации и онбординга

Тестирование выполнялось в браузерах Chrome 124 и Firefox 125 на десктопе, а также в мобильном браузере Chrome Android.

Таблица 4.6 -- Тестирование сценария регистрации и онбординга

| № | Шаг | Ожидаемый результат | Результат |
|---|---|---|---|
| 1 | Открыть страницу регистрации | Отображается форма с полями email, пароль, имя | Пройден |
| 2 | Отправить форму с некорректным email | Отображается сообщение об ошибке валидации | Пройден |
| 3 | Успешная регистрация | Перенаправление на /onboarding | Пройден |
| 4 | Прокрутка сетки онбординга | Автозагрузка следующих кандидатов | Пройден |
| 5 | Выбрать менее 12 объектов | Кнопка «Продолжить» недоступна | Пройден |
| 6 | Выбрать 12+ объектов | Кнопка доступна, прогресс-бар заполнен | Пройден |
| 7 | Нажать «Продолжить» | Сохранение профиля, перенаправление на главную | Пройден |
| 8 | Главная страница | Витрины рекомендаций с контентом | Пройден |

### 4.4.2 Тестирование сценария выставления оценки

Таблица 4.7 -- Тестирование функции оценки

| № | Шаг | Ожидаемый результат | Результат |
|---|---|---|---|
| 1 | Навести курсор на карточку | Оверлей с кнопками лайк/дизлайк | Пройден |
| 2 | Нажать «Лайк» | Кнопка активируется (синяя), toast «Лайк сохранён» | Пройден |
| 3 | Повторно нажать «Лайк» | Flash-эффект, без нового запроса к API | Пройден |
| 4 | Нажать «Дизлайк» | Смена оценки, toast «Дизлайк сохранён» | Пройден |
| 5 | Проверить карточку того же объекта в другой строке | Отображает актуальное значение (событие emz:rating) | Пройден |
| 6 | Проверить историю в профиле | Объект отображается в истории с корректной оценкой | Пройден |

### 4.4.3 Тестирование темной/светлой темы

Таблица 4.8 -- Тестирование переключения темы

| № | Шаг | Ожидаемый результат | Результат |
|---|---|---|---|
| 1 | Первый запуск при системной темной теме | Автоматически применяется темная тема | Пройден |
| 2 | Нажать кнопку переключения темы | Плавный переход в светлую тему | Пройден |
| 3 | Перезагрузить страницу | Тема сохраняется (localStorage) | Пройден |
| 4 | Сменить тему в настройках | CustomEvent emz:theme применяется глобально | Пройден |

### 4.4.4 Тестирование Telegram-бота

Таблица 4.9 -- Тестирование Telegram-бота

| № | Шаг | Ожидаемый результат | Результат |
|---|---|---|---|
| 1 | Отправить /start (новый пользователь) | Запрос email | Пройден |
| 2 | Ввести корректный email | Привязка аккаунта, главное меню | Пройден |
| 3 | Нажать «Что посмотреть?» | Карточка рекомендации с кнопками | Пройден |
| 4 | Нажать «Выбрать настроение» | Клавиатура выбора настроения | Пройден |
| 5 | Выбрать «Веселое» | Рекомендация под настроение fun | Пройден |
| 6 | Нажать «Лайк» в боте | Оценка синхронизируется с веб-профилем | Пройден |
| 7 | Нажать «История» | Последние 5 оценок | Пройден |
| 8 | Нажать ссылку «На сайте» | Открывается корректная страница детали | Пройден |

## 4.5 Оценка производительности

Тестирование производительности проводилось на локальной рабочей станции (8 ядер, 16 ГБ ОЗУ) при запуске полного стека через Docker Compose.

Таблица 4.10 -- Среднее время ответа основных эндпоинтов

| Эндпоинт | Среднее время ответа | Примечание |
|---|---|---|
| GET /api/users/me | 15 мс | Одиночный запрос к MongoDB |
| POST /api/actions/rate | 35 мс | Запрос к MongoDB + опционально TMDB |
| GET /api/movies/lists/trending | 8 мс (кэш) / 380 мс (TMDB) | 5 мин кэш |
| GET /api/recommendations/for-you | 25 мс (кэш) / 210 мс (ML+MongoDB) | 120 с кэш |
| GET /api/recommendations/because | 45 мс (кэш) / 280 мс | ML + гидрация |
| GET /health | 2 мс | Без обращения к БД |

Время ответа эндпоинтов рекомендаций при кэш-промахе включает: запрос к MongoDB для загрузки оценок (~15 мс), HTTP-запрос к ML Service (~50–120 мс в зависимости от размера базы) и гидрацию из MongoDB (~30–80 мс). Суммарное время в пределах 300 мс является приемлемым для пользовательских сценариев.

## 4.6 Выводы по четвертой главе

Проведенное тестирование подтвердило функциональную корректность всех реализованных компонентов системы EMZ View. Все тестовые случаи модуля аутентификации, оценок и рекомендаций были успешно пройдены, включая сценарии деградации при недоступности ML-сервиса. Тестирование пользовательского интерфейса подтвердило корректную работу UX-сценариев в браузерах и Telegram-клиенте. Оценка производительности показала, что механизм кэширования позволяет снизить среднее время ответа на повторные запросы рекомендаций с 210 мс до 25 мс.


# Заключение

В рамках настоящей выпускной квалификационной работы разработана и реализована гибридная рекомендательная система фильмов и сериалов EMZ View, обеспечивающая персонализированную выдачу контента, устойчивость при холодном старте и мультиплатформенное взаимодействие.

В ходе работы решены следующие задачи:

1. Проведен анализ предметной области рекомендательных систем, выявлены ограничения контентного и коллаборативного подходов. Обоснована целесообразность гибридной стратегии, использующей контентную фильтрацию при холодном старте и переключающейся на коллаборативную фильтрацию по мере накопления данных пользователя.

2. Сформулированы функциональные и нефункциональные требования к системе: 7 функциональных требований (аутентификация, онбординг, четыре витрины рекомендаций, история, синхронизация между каналами) и 6 нефункциональных требований (производительность, надежность, безопасность, масштабируемость, UX, сопровождаемость).

3. Спроектирована микросервисная архитектура с разделением на четыре независимых компонента: Frontend (React + Vite), Backend (Node.js + Express + MongoDB), ML Service (Python + FastAPI) и Telegram-бот (Telegraf). Разработаны API-контракты, структуры данных трех коллекций MongoDB, UML-диаграммы ключевых сценариев.

4. Реализован ML-компонент, включающий: TF-IDF векторизацию с параметрами `max_features=50 000`, `ngram_range=(1, 2)` для контентного профилирования; SVD-матричную факторизацию с `k=32` латентными факторами для коллаборативной фильтрации; user-user CF с косинусным сходством для витрины «Похожий вкус»; text-query поиск по TF-IDF модели для витрины «По настроению».

5. Реализован Backend-сервис с многоуровневой защитой (rate limiting, Zod-валидация, JWT), механизмом детерминированной инвалидации кэша на основе подписи состояния оценок и каскадным fallback-механизмом при недоступности ML Service.

6. Реализован Frontend с 10 страницами, оптимистичным обновлением UI при выставлении оценок, debounced-поиском, темной/светлой темой, бесконечной прокруткой в онбординге через `IntersectionObserver`.

7. Реализован Telegram-бот с inline-клавиатурой, поддержкой четырех настроений и синхронизацией оценок с веб-профилем.

8. Проведено функциональное тестирование всех эндпоинтов Backend API и ML-сервиса, тестирование пользовательского интерфейса и оценка производительности. Все тестовые случаи успешно пройдены. Механизм кэширования снижает время ответа на повторные запросы рекомендаций с 210 мс до 25 мс.

Практическая значимость работы состоит в том, что разработанная система может быть использована как основа для коммерческого сервиса персонализированного подбора контента, а её архитектура служит референсным образцом микросервисного приложения с ML-компонентом для прикладных и исследовательских проектов.

Направления дальнейшего развития системы:
- Реализация A/B-тестирования рекомендательных стратегий для оценки их влияния на пользовательское вовлечение.
- Внедрение инкрементального обновления ML-модели без полного перестроения при поступлении новых оценок.
- Расширение объяснимости рекомендаций до уровня атрибутов (конкретные жанры или актеры, определившие рекомендацию).
- Добавление поддержки Push-уведомлений и еженедельных дайджестов рекомендаций.


# Список использованных источников

1. Ricci F., Rokach L., Shapira B. Recommender Systems Handbook. -- 3rd ed. -- New York: Springer, 2022. -- 1124 с.

2. Koren Y., Bell R., Volinsky C. Matrix Factorization Techniques for Recommender Systems // IEEE Computer. -- 2009. -- Vol. 42, No. 8. -- P. 30--37.

3. Burke R. Hybrid Recommender Systems: Survey and Experiments // User Modeling and User-Adapted Interaction. -- 2002. -- Vol. 12, No. 4. -- P. 331--370.

4. Aggarwal C.C. Recommender Systems: The Textbook. -- Cham: Springer, 2016. -- 498 с.

5. Schäfer J.B., Frankowski D., Herlocker J., Sen S. Collaborative Filtering Recommender Systems // The Adaptive Web. Lecture Notes in Computer Science. -- Berlin: Springer, 2007. -- Vol. 4321. -- P. 291--324.

6. Pazzani M.J., Billsus D. Content-Based Recommendation Systems // The Adaptive Web. Lecture Notes in Computer Science. -- Berlin: Springer, 2007. -- Vol. 4321. -- P. 325--341.

7. Lops P., de Gemmis M., Semeraro G. Content-based Recommender Systems: State of the Art and Trends // Recommender Systems Handbook. -- New York: Springer, 2011. -- P. 73--105.

8. Ma H., Yang H., Lyu M.R., King I. SoRec: Social Recommendation Using Probabilistic Matrix Factorization // Proceedings of the 17th ACM CIKM. -- 2008. -- P. 931--940.

9. Scikit-learn: Machine Learning in Python / Pedregosa F. et al. // Journal of Machine Learning Research. -- 2011. -- Vol. 12. -- P. 2825--2830.

10. Документация FastAPI. URL: https://fastapi.tiangolo.com (дата обращения: 01.04.2025).

11. Документация React. URL: https://react.dev (дата обращения: 01.04.2025).

12. Документация Express.js. URL: https://expressjs.com (дата обращения: 01.04.2025).

13. Документация MongoDB. URL: https://www.mongodb.com/docs (дата обращения: 01.04.2025).

14. Документация Telegraf. URL: https://telegraf.js.org (дата обращения: 01.04.2025).

15. TMDB API Reference. URL: https://developer.themoviedb.org/reference (дата обращения: 01.04.2025).

16. Zod: TypeScript-first schema validation. URL: https://zod.dev (дата обращения: 01.04.2025).

17. Документация Docker Compose. URL: https://docs.docker.com/compose (дата обращения: 01.04.2025).

18. Resnick P., Varian H.R. Recommender Systems // Communications of the ACM. -- 1997. -- Vol. 40, No. 3. -- P. 56--58.

19. Zhang S., Yao L., Sun A., Tay Y. Deep Learning Based Recommender System: A Survey and New Perspectives // ACM Computing Surveys. -- 2019. -- Vol. 52, No. 1. -- P. 1--38.

20. Стасюк А., Котляров А. Разработка рекомендательных систем для видеосервисов на основе гибридных методов фильтрации // Труды МФТИ. -- 2022. -- Т. 14, № 3. -- С. 74--85.

